<p>生成された画像は、自動で保存されません。投稿される場合にはご自身の媒体に保存ください</p>

<%= form_with url: dreams_path, method: :post, model: nil, local: true do |form| %>
  <%= form.text_field :prompt %><br/>
  <%= form.submit '画像を生成する', class: 'btn btn-primary' %>
  <%= link_to '作品を出展する', new_board_path, class: 'btn btn-primary' %>
<% end %>

<% if @error %>
  <p><%= @error %></p>
<% elsif @image_url %>
  <img src="<%= @image_url %>" alt="Generated Image">
<% else %>
  <p>まだ画像が出力されていません。</p>
<% end %>

