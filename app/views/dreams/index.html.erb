<% content_for :title, page_title("Create") %>
<div class="w-5/6 mx-auto max-w-screen-xl">
  <h1 class="font-per flex justify-center text-5xl mb-10">Create images</h1>
    <p class= "font-kos flex justify-center">生成された画像は、自動で保存されません。出展する際には必ずご自身の媒体に保存ください。</p>
    <%= form_with url: dreams_path, method: :post, model: nil, local: true do |form| %>
    <%= form.text_field :prompt %><br/>
    <div class= "flex justify-between lg:justify-center lg:space-x-10">
    <%= form.submit '画像を生成する', class: 'btn btn-primary mb-10' %>
    <%= link_to '作品を出展する', new_board_path, class: 'btn btn-primary mb-10' %>
  </div>
<% end %>
</div>

<div class="w-5/6 mx-auto max-w-screen-xl flex justify-center mb-10">
    <% if @error %>
      <p><%= @error %></p>
    <% elsif @image_url %>
      <img src="<%= @image_url %>" alt="Generated Image">
    <% else %>
      <p>まだ画像が出力されていません。</p>
    <% end %>
    </div>
</div>


